// 1 ex

const legalAge = 18;
const guyAge = 34;
const canDrive = (guyAge >= legalAge) ? "Yes, you can drive! Hope you have a license" : "Nope, you're too young!";
console.log(canDrive);

// 2 ex

const legalSpeed = 130;
const currentSpeed =126;
const speedCheck = (currentSpeed <= legalAge) ? "You're going too fast - slow down!" : "You're driving below the speed limit";
console.log(speedCheck);

//  3 ex

const person = 26;
const age = 21;
const personAge = (person <= age) ? "Here's some juice" : "Here's some wine";
console.log(personAge);
